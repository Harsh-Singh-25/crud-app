<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Create User | Admin Panel</title>
  <link rel="stylesheet" href="/stylesheet/style.css" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-zinc-900 via-zinc-950 to-zinc-900 text-white min-h-screen flex items-center justify-center">

  <div class="w-full max-w-md bg-zinc-800/90 rounded-2xl shadow-2xl p-10 border border-zinc-700 backdrop-blur-sm">
    <!-- Navigation Header -->
    <nav class="mb-8 flex items-center justify-between">
      <a href="/read" class="text-zinc-400 hover:text-blue-400 font-medium text-sm transition">← All Users</a>
      <span class="text-zinc-400 text-xs uppercase tracking-wide">Admin Panel</span>
    </nav>

    <h1 class="text-3xl font-extrabold tracking-tight mb-8 text-center text-blue-400">Create User</h1>
    <form action="/create" method="post" class="space-y-6">

      <div>
        <label for="name" class="block text-sm font-medium mb-1">Name</label>
        <input
          id="name"
          name="name"
          type="text"
          required
          placeholder="Enter full name"
          autocomplete="name"
          class="w-full px-4 py-3 border border-zinc-700 rounded-lg bg-zinc-900/80 text-white placeholder-zinc-500 focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition"
        />
      </div>

      <div>
        <label for="email" class="block text-sm font-medium mb-1">Email</label>
        <input
          id="email"
          name="email"
          type="email"
          required
          placeholder="Enter email address"
          autocomplete="email"
          class="w-full px-4 py-3 border border-zinc-700 rounded-lg bg-zinc-900/80 text-white placeholder-zinc-500 focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition"
        />
      </div>

      <div>
        <label for="imageUrl" class="block text-sm font-medium mb-1">Image URL</label>
        <input
        id="imageUrl"
        name="imageUrl"
        type="text"
          placeholder="Enter an image URL"
          autocomplete="off"
          class="w-full px-4 py-3 border border-zinc-700 rounded-lg bg-zinc-900/80 text-white placeholder-zinc-500 focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition"
        />
      </div>

      <div class="pt-3">
        <button
          type="submit"
          class="w-full py-3 bg-blue-600 hover:bg-blue-700 transition text-white font-semibold rounded-lg shadow-md focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-offset-2"
        >
          Create User
        </button>
      </div>
    </form>
  </div>

</body>
</html>
